{
  "name": "bcsave",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "fetch:dogs": "node scripts/fetch-dogs.mjs",
    "fetch:alumni": "node scripts/fetch-alumni.mjs",
    "build": "npm run fetch:dogs && astro build",
    "build:offline": "astro build",
    "deploy": "npm run build && wrangler deploy",
    "preview": "astro preview",
    "astro": "astro",
    "audit": "node scripts/audit.mjs"
  },
  "dependencies": {
    "astro": "^7.1.6",
    "sharp": "^0.34.4"
  },
  "devDependencies": {
    "puppeteer-core": "^25.6.0",
    "wrangler": "^4.118.0"
  }
}
